<!doctype html>
<html>
    <head>
        <meta chatset="utf-8">
        <title>お知らせ編集</title>
        <link rel="stylesheet" href="/css/style.css">
    </head>
    <body>
        <h2>お知らせ編集</h2>
        
        <form action="admin/news/edit/<%= news.id %>" method="post" class="form-container">
            <div class="form-group">
                <label for="title">タイトル</label>
                <input type="text" id="title" name="title" value="<%= news.title %>" required>
            </div>

            <div class="form-group">
                <label for="content">内容</label>
                <texarea id="content" name="content" rows="5" required><%= news.content %></texarea>
            </div>
            
            <button type="submit" class="submit-btn">更新する</button>
        </form>
        <br>
        <a href="/">キャンセル</a>
    </body>
</html>